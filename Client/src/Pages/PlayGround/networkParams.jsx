let radius = 10;
let strokeW = 1;
let maxHeightY = 500;
let maxHeightX = 500;
let offsetX = 0;
let offsetY = 0;
let layerDistance = 100;
let neuronDistance = radius * 4;

// here I should make a redux store

export {
  radius,
  strokeW,
  maxHeightY,
  maxHeightX,
  offsetX,
  offsetY,
  layerDistance,
  neuronDistance,
};
